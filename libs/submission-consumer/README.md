# submission-consumer

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build submission-consumer` to build the library.

## Running unit tests

Run `nx test submission-consumer` to execute the unit tests via [Jest](https://jestjs.io).
